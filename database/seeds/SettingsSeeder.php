<?php

use Illuminate\Database\Seeder;

class SettingsSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // DB::table('settings')->delete();
        // $inputData = json_decode(file_get_contents(storage_path('settings.json')));
        // $data = [];

        // foreach ($inputData as $setting) {
        //     $data[] = [
        //         'key' => $setting->key,
        //         'value' => $setting->value,
        //     ];
        // }

        // if (!empty($data)) {
        //     DB::table('settings')->insert($data);
        // }
    }
}
